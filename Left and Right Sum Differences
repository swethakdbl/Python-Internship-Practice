class Solution:
    def leftRightDifference(self, nums: List[int]) -> List[int]:
        total_sum=sum(nums)
        leftsum=0
        result=[]
        for num in nums:
            total_sum-=num
            result.append(abs(leftSum-total_sum))
            left_sum+=num
        return result
