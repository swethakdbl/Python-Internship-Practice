class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        d = {}     
        c = 0
        for num in nums:
            if num in d:
                c += d[num]   
                d[num] += 1
            else:
                d[num] = 1
        return c
